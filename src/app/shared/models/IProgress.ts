export interface IProgress {
  percent: number; // Porcentagem de episódios assistidos
  totalEpisodes: number; // Total de episódios
  watchedEpisodes: number; // Quantidade de episódios assistidos
}
